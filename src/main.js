import { invoke } from '@tauri-apps/api/core';

console.log('Perch frontend loaded');

// Placeholder for future UI interaction
document.addEventListener('DOMContentLoaded', async () => {
  console.log('Perch is running');
});
